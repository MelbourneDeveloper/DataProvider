---
layout: layouts/base.njk
title: Blog
description: Latest news, tutorials, and updates from the DataProvider team.
---

<section style="padding: var(--space-12) 0;">
  <div class="container">
    <div class="section-title">
      <h1>Blogs</h1>
      <p>Latest news, tutorials, and updates from the DataProvider team.</p>
    </div>

    <div style="max-width: 400px; margin: 0 auto var(--space-8);">
      <input type="text" class="search-input" placeholder="Search blogs..." id="blog-search">
    </div>

    <div class="filter-tabs" style="justify-content: center;">
      <button class="filter-tab active">All Topics</button>
      <button class="filter-tab">.NET</button>
      <button class="filter-tab">C#</button>
      <button class="filter-tab">SQL</button>
      <button class="filter-tab">LQL</button>
      <button class="filter-tab">Database</button>
    </div>

    <div class="blog-grid">
      {% for post in collections.posts %}
      <article class="blog-card">
        <div class="blog-card-image">
          {% if post.data.image %}
          <img src="{{ post.data.image }}" alt="{{ post.data.title }}">
          {% endif %}
        </div>
        <div class="blog-card-content">
          <p class="blog-card-meta">
            <time datetime="{{ post.date | isoDate }}">{{ post.date | dateFormat }}</time>
          </p>
          <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
          <p>{{ post.data.description }}</p>
          {% if post.data.tags %}
          <div style="margin-top: var(--space-3);">
            {% for tag in post.data.tags %}
            {% if tag != 'post' and tag != 'posts' %}
            <a href="/blog/tags/{{ tag | slugify }}/" class="tag">{{ tag }}</a>
            {% endif %}
            {% endfor %}
          </div>
          {% endif %}
        </div>
      </article>
      {% endfor %}
    </div>

    {% if collections.posts.length == 0 %}
    <div class="text-center" style="padding: var(--space-16) 0;">
      <p>No blog posts yet. Check back soon!</p>
    </div>
    {% endif %}
  </div>
</section>
